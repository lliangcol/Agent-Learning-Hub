# ADR-0007：Harness 评测基线

- 状态：Accepted
- 日期：2026-07-20

## 决策

V2 离线核心使用仓库内的类型化 `AgentRunner` 作为现代 Harness 基线，与最小裸循环比较。它具备状态、停止原因、工具注册、预算、重复调用检测和 Trace，且不引入 API key 或外部服务。

外部 Harness 属于可选扩展。任何版本接入前必须锁定依赖，并在相同 20 条固定数据集上比较成功率、失败类型、步骤、延迟与成本字段。
