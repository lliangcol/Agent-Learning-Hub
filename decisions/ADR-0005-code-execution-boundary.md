# ADR-0005：代码执行边界

- 状态：Accepted
- 日期：2026-07-20

## 决策

核心课程不执行模型或用户提供的任意不可信代码。普通子进程加超时只能称为教学演示，不能称为安全沙箱。

可选容器实验必须禁网络、使用只读根文件系统、临时工作区、非 root 用户，并限制 CPU、内存、进程、时间和输出。Docker/WSL 不可用时明确跳过。

## 后果

- 删除模型控制输入路径中的危险 `eval()`/`exec()`。
- 安全计算器只接受受限 AST。
- 任何扩大执行权限的变更都需要新的安全 review 和明确批准。
