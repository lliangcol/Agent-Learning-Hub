# v2.0.0-rc.1 发布候选

状态：review 修复位于未提交工作区；尚未提交、推送、打 tag 或部署。

## 候选范围

- 将 README-first V1 重构为结构化课程、可执行 Labs、证据化进度和 MkDocs 站点。
- 保留 V1 资产、旧 localStorage 键和当前 origin 的只读导出入口。
- 修复危险计算器求值、Windows 工具导入失败和“普通子进程即沙箱”的错误表述。
- 增加离线 provider、RAG、SQLite 记忆、策略、trace、eval、Skill、浏览器、
  多 Agent 和 Capstone 基线。
- 增加 Windows/Ubuntu CI、供应链审计、Pages preview/deploy、链接和上游审计。

## 本地候选证据（2026-07-20，Windows）

- Python 3.12：91 tests 通过，另有 1 个需显式 opt-in 的容器 smoke 按设计
  skip；整体分支覆盖率 92.99%，关键安全模块 100%。
- Python 3.11.15 和 3.14.5：隔离环境各 91 tests 通过；CI 使用浮动
  `3.11`/`3.14` 补齐平台和补丁版本证据。
- 前端：3 个测试文件、16 tests 通过。
- 浏览器：系统 Chrome 150 上 31 通过、8 个按矩阵设计跳过；无失败。
- Ruff、mypy、Markdown、严格 MkDocs、内部链接、内容 schema、生成物和性能预算通过。
- 依赖：Python 已知漏洞 0；npm production 已知漏洞 0。
- 可复现构建准备检查：连续两次构建的 67 个文件一致，排序清单摘要为
  `70fbe914c6ac12b7d407b3729755f05c4113b3fa9163b55471d15b6b78d6f65d`。
- 供应链：pip/npm 已知漏洞 0。此前截至 `cba4191` 的 actionlint 与 gitleaks 证据
  不覆盖本轮未提交修复；当前候选为 227 个已跟踪文件加 2 个待跟踪文件，须由提交后的
  供应链 CI 复验。

上述摘要来自 `0ad2da4` 之上的当前未提交修复工作区。正式 RC 必须在提交后从全新
checkout 复验，并记录源 commit、CI run 和 Pages preview artifact 标识。

## 未满足的发布门

- V1 origin 已确认：`file://` 与 `http://localhost:8000` 无数据，GitHub Pages
  从未使用；若后续发现其他 origin，必须在 RC 前补充登记。
- 维护者已指定 `lliang@outlook.com` 为私下安全报告入口；GitHub Private
  Vulnerability Reporting 保持 disabled，文档不声称已启用。
- 真实屏幕阅读器抽查尚未完成。
- 分支已有此前的本地 commits，但本轮 review 修复尚未 commit；尚未获得 commit、push、
  `v2.0.0-rc.1` tag 或 Pages 部署授权。
- 尚无全新 Ubuntu checkout/CI、preview artifact 和基于真实源 commit 的回滚演练证据。
- 正式发布前还需再次确认变更范围和提交历史。

## 已知限制

- 离线 mock 证据不代表真实 provider 调用通过。
- 外链审计中的 bot 403 或本地证书链失败保持 `suspect`，不冒充失效链接。
- Material for MkDocs 9.x 已锁定；MkDocs 2.0 兼容性不在本候选范围内。
- 固定 Playwright Chromium 1228 本地下载在 5 分钟内无响应；本轮浏览器门禁
  使用系统 Chrome 150。CI 仍须用锁定的 Playwright Chromium 生成正式证据。
- legacy 资产不会在本次候选或正式发布后自动删除。
