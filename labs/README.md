# Labs

每个 Lab 使用同一结构：`starter/`、`solution/`、`tests/`、`fixtures/` 和 `expected/`。默认路径离线运行；可选真实 Provider、Docker 或浏览器能力必须明确说明前提和跳过条件。

运行单个 Lab：

```powershell
uv run pytest labs/stage-02-agent-loop/tests
```
